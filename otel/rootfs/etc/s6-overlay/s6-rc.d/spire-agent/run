#!/usr/bin/with-contenv bashio
set -Eeuo pipefail

mkdir -p /data/spire /run/spire

bashio::log.info "Starting SPIRE agent"
exec s6-notifyoncheck -n 0 -w 2000 \
  /usr/local/bin/spire-agent run -config /run/spire/agent.conf
